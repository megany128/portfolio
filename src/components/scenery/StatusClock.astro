---
// Bottom-left live clock + status line, persistent across welcome/onboarding scenes.
---

<div
  class="status-clock pointer-events-none absolute bottom-[21px] left-[21px] font-[family-name:var(--font-mono)] text-base uppercase text-[var(--color-ink-mute)] leading-tight hidden sm:block"
  data-status-clock
>
  <p data-clock>--:--:-- --</p>
</div>

<script>
  function tick() {
    const el = document.querySelector<HTMLElement>("[data-clock]");
    if (!el) return;
    const now = new Date();
    const h12 = ((now.getHours() + 11) % 12) + 1;
    const mm = String(now.getMinutes()).padStart(2, "0");
    const ss = String(now.getSeconds()).padStart(2, "0");
    const ampm = now.getHours() >= 12 ? "PM" : "AM";
    el.textContent = `${h12}:${mm}:${ss} ${ampm}`;
  }
  tick();
  setInterval(tick, 1000);
</script>
