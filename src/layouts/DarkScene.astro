---
import BaseLayout from "./BaseLayout.astro";
import Sparkles from "../components/scenery/Sparkles.astro";
import StatusClock from "../components/scenery/StatusClock.astro";

interface Props {
  title?: string;
  description?: string;
  image?: string;
  showSparkles?: boolean;
  showClock?: boolean;
}

const {
  title,
  description,
  image,
  showSparkles = true,
  showClock = true,
} = Astro.props;
---

<BaseLayout title={title} description={description} image={image} bodyClass="bg-[var(--color-ink)] text-[var(--color-ink-inverted)]" showPet={false}>
  <div class="relative min-h-dvh overflow-hidden">
    {showSparkles && <Sparkles />}
    <slot />
    {showClock && <StatusClock />}
  </div>
</BaseLayout>
